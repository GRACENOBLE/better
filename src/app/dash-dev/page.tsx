import Chat from "@/components/ai/chat";
import Container from "@/components/common/container";

const page = () => {
  return (
    <section>
      <Container>
        <Chat />
      </Container>
    </section>
  );
};

export default page;
